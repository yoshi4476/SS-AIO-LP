パイプライン実行（自動実行モード）。CLAUDE.md の Phase 1〜7 を順番に実行して新規記事を1本作成してください。ルール: (1) kpi_feedback.md があれば冒頭で読み込む (2) Phase 3 のユーザー確認はスキップ（自動実行モード） (3) API・MCP が未設定/未認証の工程は10分ポーリングせずスキップしてエラーログに記録し、利用可能な情報だけで続行 (4) Phase 5 の品質基準（score 90点以上）を満たした場合のみ articles/ に保存して scripts/build.py を実行 (5) git push はリモート設定済みの場合のみ実行、未設定ならコミットまで (6) 実行結果の要約を automation/logs/ 内の本日のログに残す。
速度規律（品質基準は維持したまま最短経路で）: (i) 読み込むファイルは CLAUDE.md / PROJECT.md / kpi_feedback.md / docs/industry-pillar-plan.md / articles/ のファイル名一覧＋選定KWに関係する既存記事のみに限定し、探索的なファイル閲覧・サイト内HTMLの読み込みを行わない (ii) Phase 3 の構成事前審査は1回の思考でまとめて14項目を判定する (iii) Phase 5 のLLM採点は6観点を1回の応答でまとめて採点し、修正は指摘箇所のピンポイント編集のみ行う（全文書き直し禁止） (iv) 執筆は構成確定後に一気に書き切る（節ごとの確認往復をしない） (v) ビルド・画像生成はコマンド実行のみで、出力の全文精読はエラー時だけ行う。
採点ルール: Phase 5 では必ず先に `python scripts/score_check.py <slug>` を実行し、全項目PASSにしてからLLM採点（6エージェント）に進む。採点は「批判先出し（欠点3つ以上）→採点」の順で行い、フロントマターに score と score_breakdown の両方を記録する。いずれかの観点が16/20未満なら合計に関わらず不合格として修正する。
品質の作り込みルール: Phase 3 の「構成事前審査（14項目）」を必ず実施し、全項目○になるまで構成を修正してから執筆に入ること。執筆後の修正ループ（Phase 5）は保険であり、構成段階で90点の要件を満たし切るのが本筋。Phase 5で不合格になった場合は、その原因が構成審査のどの項目の見落としだったかをログに記録すること（次回の構成審査の精度向上に使う）。
KW選定の追加ルール: (a) Phase 1 の最初に必ず `python scripts/kw_status.py` を実行し、その出力（KW_REMAINING / 次のKW候補 / NEED_REPLENISH）に従う。表示された「次のKW候補」の一番上を今回のKWとして採用する（自分でリストを読み直して選び直さない） (b) 既存記事のタイトル・スラッグと重複・カニバリしないことを articles/*.md を確認してから確定する (c) シェアボタン・診断バナー・前後ナビ・購読フォームはビルドが自動挿入するため、記事Markdownに手動で書かないこと (d) 【KW自動補充】kw_status.py が NEED_REPLENISH=yes を出した場合は、記事作成の前に docs/industry-pillar-plan.md へ次の30KWを追記設計する（既存の「**〜（10本）**: KW / KW / …」と同じ行形式で書くこと。形式が違うとスクリプトが読み取れない）。設計基準: 既存記事・既存KWと重複しない / G-ranの強み（MEO・店舗集客）に隣接する業種・課題KWを優先 / ロングテール（月間検索100〜1,000想定）中心 / 各KWに想定カテゴリと狙い（1行）を添える。追記後にそのまま通常のKW選定へ進む。
