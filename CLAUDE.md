# SEOオウンドメディア 全自動パイプライン【AIO/LLMO/GEO対応版】

> 記事制作をKW選定から公開後処理まで全自動化する。人間は「品質チェックの最終承認」と「事業方向性の決定」のみ。
> プロジェクト固有の設定（ドメイン・ペルソナ・カテゴリ・CTA等）は [PROJECT.md](PROJECT.md) を参照。**各Phaseの実行前に必ず PROJECT.md を読み込むこと。**

## トリガーコマンド

- 「記事を書いて」「パイプライン実行」→ Phase 1〜7 を順番に実行
- 「KPI確認」→ Phase 7 の KPIレポート生成を実行
- 「リライト」→ 第8章の週次最適化を実行
- 「AIO監査」→ 第8.5章の AIO監査を実行

---

## 0.1 数字を出す前に必ず通す手順（最優先・例外なし）

**1つの測り方の結果を、そのまま事実として報告してはならない。**

実際に1つのセッションで9回、誤った数字を報告した。どれも30秒の再確認で防げた。
誤りの上に次の判断を重ねたため、施策の優先順位まで誤らせた。

| 報告した内容 | 根拠にしたもの | 実際 |
|:--|:--|:--|
| CTR1.6%が課題 | GSCのquery次元 | page次元では111クリック（query次元は検索数の少ない語を返さない） |
| コーポレートは73%達成 | クリック総数 | 17回中15回が指名検索。記事の力ではない |
| 転換率5.7% | form_submit + lead_capture | 同じ送信で両方発火する。二重計上で実際は4件 |
| 9%はGoogle広告 | GA4の媒体名 | 広告は未出稿。国も取れない無効な流入だった |
| JSが同期読み込み | 正規表現（srcが先と仮定） | async は src の前にあった |
| CTAゼロ58本 | `cta-button` クラスだけを数えた | 別のクラス名で導線があった |
| 191本が導線なし | 原稿のMarkdownを見た | 生成HTMLには全て入っていた（テンプレートが挿入） |
| H1がAIに読まれない | タグを空白に置換して抽出 | タグを除去すれば正しい文だった |

**共通しているのは1つだけ。「自分の測り方が正しい」と仮定したこと。**
対象の性質ではなく、道具の癖を見ていた。

### 数字を1つでも出すなら、この4つを必ず通す

| # | やること | 防げる誤り |
|:--|:--|:--|
| 1 | **別の方法で同じ数字を出す**。一致しなければ、一致するまで報告しない | 道具の癖・抽出方法の癖 |
| 2 | **内訳に分ける**。合計だけで語らない（指名検索・ボット・重複を分離する） | 水増し・二重計上 |
| 3 | **「0件」「全部」「無い」と言う前に、逆を探す**。検出できないことは、存在しないことではない | 1つの書き方しか見ていない |
| 4 | **前提を口に出して確かめる**（広告は出しているか／設定はどこにあるか） | 前提の思い込み |

### 「見つからなかった」と「無い」は違う

`grep` が0件でも、それは「その書き方では無い」であって「存在しない」ではない。
**必ず、見つかるはずの例で検出器を試してから**、0件を報告する。

### 誤った数字を、次の判断に使わない

数字が1つ崩れたら、そこから導いた結論をすべて取り下げる。
「概ね合っている」で先に進むと、誤りが施策の優先順位まで運ばれる。

### 使う道具

```bash
python scripts/data_sanity.py      # 二重計上・無効流入・GA4とGSCの食い違い・指名検索の割合
```

ここで注意が出た数字は、原因を確かめるまで報告しない。

### GSCとGA4で、特に間違えやすいところ

| 落とし穴 | 正しい扱い |
|:--|:--|
| GSCのquery次元 | 検索数の少ない語を返さない。**合計は次元なしかpage次元で取る** |
| GSCのpage次元 | 末尾スラッシュ違いで同じページが2行に分かれる。**合算する** |
| URLの末尾だけをslugにする | 別カテゴリの同名ページが1つに潰れる。**URLごと鍵にする** |
| GA4のクリック・セッション | ボット（国が取れない）と指名検索を**必ず分けて見る** |
| 1回の送信で飛ぶイベント | `form_submit` と `lead_capture` は**同時に飛ぶ。足さない** |

---

## 0. 用語整理（AIO / LLMO / GEO / AEO）

| 用語 | 対象 | 本パイプラインでの扱い |
|:--|:--|:--|
| AIO（AI Overview Optimization） | Google検索のAI Overview・AIモードでの表示/引用獲得 | 主戦場。GSCの生成AIパフォーマンスレポートで計測 |
| LLMO（Large Language Model Optimization） | ChatGPT・Perplexity・Gemini・Claude・Copilot等での引用獲得 | AIOと同時に対策。GA4のAI参照元で計測 |
| GEO（Generative Engine Optimization） | 生成エンジン全般への最適化（AIO+LLMOの総称） | AIO/LLMOの上位概念として使用 |
| AEO（Answer Engine Optimization） | 回答エンジン（強調スニペット等含む）への最適化 | AIOの施策に包含 |

**基本方針**: AIOとSEOは対立しない。Google上位表示（SEO）がAI Overview引用の前提条件であり、「SEOで上位を取り、その構造でAIにも引用される」二段構えで設計する。

### プラットフォーム別の重視シグナル

| プラットフォーム | 最重視シグナル | 対応Phase |
|:--|:--|:--|
| Google AI Overview / AIモード | Google上位表示+構造化データ+E-E-A-T+抽出しやすい構造 | Phase 3〜6 |
| Gemini | Google上位+FAQPage/HowToスキーマ+E-E-A-T | Phase 5〜6 |
| ChatGPT（SearchGPT含む） | ドメイン権威・繰り返し引用・被リンク・メディア露出 | Phase 7（外部評価の蓄積） |
| Perplexity | 情報鮮度・信頼性・出典明記・速いインデックス | Phase 6〜7（即時インデックス+定期更新） |
| Claude | 正確性・著者信頼性 | Phase 5（E-E-A-T注入） |

---

## 1. 概要

### 自動化される工程

1. **KW選定エージェント**: Ahrefs APIで競合KW抽出→3C分析→AI Overview表示状況チェック→SEO Knowledge批判3周→確定
2. **リサーチエージェント**: X API v2で関連投稿30件+YouTube文字起こし5-10本（一次情報=AI引用獲得の源泉）
3. **設計エージェント**: 検索上位10記事のH2構造分析+AI Overview引用元分析→共通60-70%+独自30-40%の構成確定
4. **執筆エージェント**: 80%ドラフト→一次情報20%組み込み→AI感排除→AIO対応ライティング→SEO Knowledge品質批判10-20周
5. **品質エージェント**: 6エージェント並列採点（120点満点中114点以上=95%が公開条件）
6. **公開エージェント**: アイキャッチ+H2直下図解→HTML生成（テンプレ適用+Schema直書き）→sitemap.xml/llms.txt更新→Cloudflare Pagesへデプロイ
7. **分析エージェント**: インデックス登録+IndexNow送信→GSC生成AIパフォーマンス計測→スプレッドシート更新→内部リンク自動追加→KPIレポート

### 期待される成果

- 1サイトあたり毎日2本・**月60本を上限**として高品質SEO記事を公開
  - **受け入れられるのは10社まで**。日次の枠は20（1社2本×10社）で、
    11社目からは全社の本数が減る。超えると実行のたびにエラーで知らせるので、
    そうなったら**別リポジトリに分けて運用する**（1リポジトリに詰め込まない）
  - 枠は「番号をサイト数で割った余り」でサイトに割り当てる。番号を直接書くと、
    サイトを増やしたときに選ばれない社が出て、記事が1本も書かれないまま放置される
  - 31日ある月は2本/日で62本になるため、月末は自動で頭打ちになる
  - 同一ドメインへ短期に大量投入すると機械的な生成と見なされる risk があるため、上限は `scripts/daily_audit.py` の `MONTHLY_CAP` で強制する（配信の入口で止まる）
- AIO/GEO/LLMO対応（AI Overview・AIモードでの引用獲得率向上、AI経由流入の獲得）
- ゼロクリック検索時代に備えた「引用される資産」と「指名検索・CV導線」の同時構築

---

## 2. 前提条件

### 必須ツール/API

| ツール | 用途 | 備考 |
|:--|:--|:--|
| Claude Code | パイプライン実行基盤 | Scheduled Tasks / cron で定期実行 |
| 静的HTMLサイト（Cloudflare Pages） | 記事公開先 | Gitリポジトリ連携で自動デプロイ。メタ情報・SchemaはHTMLテンプレートに直書き（WordPress/Rank Math不使用） |
| Ahrefs API（MCP経由） | KW調査・競合分析・SERP分析・AI Overview表示確認 | MCP設定が必要 |
| X(Twitter) API v2 | 一次情報収集（投稿） | Bearer Token必須。note_tweetフィールド対応 |
| YouTube Data API v3 | 一次情報収集（動画） | APIキー必須 |
| Google Sheets API | データ管理・KPIレポート | MCP経由推奨 |
| Google Indexing API | 即時インデックス登録 | サービスアカウント+GSCオーナー権限 |
| Google Search Console | 検索計測+生成AIパフォーマンスレポート（2026年6月導入） | AIO計測の中核 |
| IndexNow（オプション） | Bing/Copilot系への即時URL通知 | キーをサイトルートに設置 |
| 画像生成（Pillow） | アイキャッチ+図解 | `scripts/make_images.py` がローカルで描画。外部APIは呼ばないため、キーの設定は不要 |
| yt-dlp | YouTube自動字幕取得 | `pip install yt-dlp` |
| Python 3.10+ | Markdown→HTML変換 | `markdown` ライブラリ（tables, extra） |

認証情報は [.env](.env) に設定する（雛形あり）。スプレッドシートIDは PROJECT.md で管理。

---

## 3. セットアップ手順

### 3.0 クライアントの導入（受託運用の入口）

**ヒアリングシートを1枚埋めれば、運用に必要なものが全部そろう。**

```bash
python scripts/client_intake.py --sheet                  # 汎用シートを作る
python scripts/client_intake.py --sheet restaurant       # 飲食店向け（業種別の項目つき）
```

**記入済みのシートは `intake/` に置くだけでよい。** ファイル名を指定し損ねたり、
不備の確認を飛ばしたりするのを防ぐため、置き場を決めて機械に拾わせる。

```bash
python scripts/intake_watch.py            # 何が起きるかを見るだけ
python scripts/intake_watch.py --apply    # 登録まで行う
```

| 結果 | 行き先 |
|:--|:--|
| 登録できた | `intake/done/` |
| 不備があった | `intake/todo/` と、同じ名前の `.不備.txt`（直す箇所を書く） |

**シートそのものはコミットされない**（`.gitignore` 済み）。会社名・住所・電話・
担当者のメールが入るうえ、このリポジトリは public のため。登録後に生成される
`sites/*.json` だけをコミットする。**10社を超えるシートは受け付けずに止める。**

1枚ずつ指定したいときは従来どおり `client_intake.py <記入済み.xlsx> --apply` も使える。

**自社の一次データも同じ置き場で公開する**（`python scripts/data_intake.py --sheet` で
`intake/データ記入シート.xlsx` を作る → 埋めて intake/ に置く → `intake_watch.py --apply`）。
`site/data/<slug>/` に表・棒グラフ・引用用の一文・Dataset 構造化データ・CSV のページができ、
llms.txt と sitemap に載り、同じカテゴリの記事末に「自社の一次データ」枠が出て、引用用の一文は
一次情報として登録される（記事から使われる）。**AIが根拠に選ぶのは「そこにしか無い数字」**で、
これが引用を取る最大の手。母数10未満・期間なし・個社名入りは公開しない。

**実績・お客様の声も同じ置き場で登録する**（`python scripts/jisseki_intake.py --sheet` で
`intake/実績記入シート.xlsx` を作る → 埋めて intake/ に置く → `intake_watch.py --apply`）。
経理BPOの効果・継続率は一次情報（記事に使われる）に、お客様の声は `data/voices.json` と
運営者情報・LP（AI集客ラボ）に載る。**数字は本人が入れたものだけ。機械は作らない。**
割合は母数10件以上・期間つきのときだけで、未満は実数に落とす（`add_fact` と同じ条件）。
掲載可否が「可」でない声は載せない。「可」にできるのは本人が書いたか、本人が内容を確認して許可した声だけ
（事業者が作った感想を「お客様の声」として表示するのはステルスマーケティング規制の違反）。
会社名を出さない場合は表示名（例: 大阪市の歯科医院）。

`--apply` で作られるもの:

| 生成物 | 中身 |
|:--|:--|
| `sites/<id>.json` | サイト設定（主力商材・カテゴリ配分・守備範囲・CTA・計測） |
| `data/clients/<id>/company.json` | 会社の正規表記（著者情報・構造化データ・レポートの宛名） |
| `data/clients/<id>/facts.json` | その会社にしか出せない一次情報 |
| `data/clients/<id>/brief.json` | 記事を書くための材料（売り物・読者の困りごと・FAQ・著者・文体・狙う語・外部接点） |
| `docs/kw-<id>.md` | KW計画の雛形 |

**記事の自動化に効く部分**: シートの回答は `site_brief.py` が執筆時に読み込む。売っているもの・読者の困りごと・よく聞かれる質問（そのままFAQになる）・著者情報・文体・メイン/サブキーワード・外部との接点まで渡るため、「その会社にしか書けない記事」になる。**書かれていないことは書かせない**（憶測で補うと事実と違う記事が公開される）。

**主題キーワードの生成**: メインKW × サブKW × 意図 × 業種 を掛け合わせて主題候補を作る（`client_intake.subjects()`）。同じ語の重なりと「狙わない語」は除外する。候補はそのまま書かず、`kw_guard`（食い合い）と `kw_intent`（開く理由）を必ず通す。

**先に進ませない条件（不備として止める）**:
- 主力商材・主力カテゴリが空 → 表示は増えても相談につながらない記事が量産される
- 一次情報が1件も無い → どのサイトでも書ける記事になり、AI検索に引用されない
- 既存サイトとカテゴリ・ドメインが重複 → 記事がどちらのものか決まらない
- 主力カテゴリの配分が35%未満 → 警告（40〜50%を推奨）

**受託運用での分離**: クライアントの記事に運用会社の実績を書くと事実と違う記事になる。`data/clients/<id>/facts.json` がある場合、`facts.py` はそちらだけを使い、自社の一次情報を混ぜない。

JSONを直接書く場合は `client_add.py`（既存クライアントの設定変更など）。

**一次情報の追加**: `scripts/add_fact.py` で登録する。JSONを手で書かない。

```bash
python scripts/add_fact.py --pending              # 何を入れれば記事が書けるか
python scripts/add_fact.py --template saitaku     # 記入用の雛形
python scripts/add_fact.py --check <file.json>    # 条件を満たすか検査
python scripts/add_fact.py --add <file.json>      # 通れば登録
```

**割合を書くなら、母数と集計期間を本文に含める。** 「採択率90%」だけでは、何社中何社か・いつからいつまでかが分からず、優良誤認になる（景品表示法・根拠の明示）。登録の時点で機械が止める。母数が10件未満なら割合では書かせず、実数で書かせる。

| 止める書き方 | 通る書き方 |
|:--|:--|
| 当社の採択率は90%です | 2025年4月〜2026年3月に支援した42社のうち38社が採択されました（採択率90.5%） |
| 多くの企業を支援してきました | 2026年9月までに42社の申請を支援しました |


### 3.0.1 納品方式（クライアントごとに選ぶ）

| 形式 | 配信先 | 品質ゲートの効き方 |
|:--|:--|:--|
| `self-static` | 本リポジトリの静的サイト | build.py がビルド対象から除外。**ファイルが存在しない** |
| `external-md` / `external-html` / `nextjs-json` | 別リポジトリの静的サイト | publish.py が90点未満を配信しない |
| `wordpress` | WordPress（REST API） | **先方の mu-plugin が公開を止める**（下記） |

**WordPress納品での品質ゲート**

静的サイトは「基準に届かない記事はファイルごと生成されない」ため物理的に公開できない。WordPressはデータベースに入るため、公開ステータスへの遷移を先方側で止める必要がある。

1. `automation/wordpress/ss-quality-gate.php` を先方の `wp-content/mu-plugins/` に置く
2. `.env` に `WP_USER_<SITE_ID>` と `WP_APP_PASSWORD_<SITE_ID>` を設定（アプリケーションパスワード）
3. `python scripts/publish.py --site <id> --slug <slug>` で投稿

mu-plugins に置くのは、**管理画面に停止ボタンが出ない**ため。「忙しかったので一旦切った」が起きない。`wp_insert_post_data` で止めるのは、管理画面・REST API・WP-CLI・予約投稿のすべてが必ず通る場所だから。個別の入口をふさぐ形にすると、入口が増えたときに漏れる。

- パイプラインが入れた記事: `_ss_quality_score` が90未満、または未設定なら下書きへ戻す
- 人が管理画面で書いた記事: `_ss_written_by=human` として、文字数と見出し数の簡易チェックのみ
- 止めた理由は管理画面に表示し、REST API経由の場合はメタに残す

**限界**: サーバーのファイルを触れる人がこのファイルを消せば外れる。ただし管理画面からは無効化できないため、事故では起きない。

### 3.1 ディレクトリ構成

```
├── CLAUDE.md               ← 本ファイル（パイプライン定義）
├── PROJECT.md              ← プロジェクト設定（要カスタマイズ）
├── .env                    ← 認証情報（.gitignore対象）
├── articles/               ← 記事マークダウン（原稿マスター）
├── site/                   ← 公開ルート（Cloudflare Pagesがこのフォルダを配信）
│   ├── index.html          ← トップページ
│   ├── lp/index.html       ← リード獲得LP（記事CTAの誘導先。CV導線の中核）
│   ├── {category}/index.html ← カテゴリ一覧（aio / seo / meo / ai-marketing）
│   ├── {category}/{slug}/index.html ← 記事HTML（ビルドで生成）
│   ├── css/style.css       ← 共通スタイル
│   ├── images/             ← 公開画像（アイキャッチ・図解）
│   ├── robots.txt          ← AIクローラー許可設定
│   ├── llms.txt            ← AIクローラー向けサイト案内
│   └── sitemap.xml         ← ビルドで自動生成
├── templates/
│   └── article.html        ← 記事HTMLテンプレート（メタ+JSON-LD Schema込み）
├── scripts/
│   └── build.py            ← Markdown→HTML変換+sitemap生成ビルドスクリプト
├── automation/
│   ├── images/             ← 生成画像（作業用）
│   ├── logs/               ← 実行ログ
│   └── design/             ← デザインルール
├── data/
│   ├── x_trends/           ← X API収集データ
│   ├── youtube_transcripts/ ← YouTube文字起こし
│   └── ai_citations/       ← AI引用スポットチェック結果
└── kpi_feedback.md          ← KPIフィードバック（自動更新）
```

### 3.2 スプレッドシート準備

Google Sheetsで11タブを用意する（列構成の詳細は第9章）:
KW戦略 / トピッククラスター / X一次情報 / YouTube一次情報 / ダッシュボード / 記事作成ログ / KPIレポート / AIO計測 / 内部リンク管理 / リライトログ / エラーログ

### 3.3 静的サイト設定（Cloudflare Pages）

1. Gitリポジトリ作成（本プロジェクト）→ Cloudflare Pages プロジェクトに接続（ビルド出力ディレクトリ: `site/`）
2. `git push` → Cloudflare Pages が自動デプロイ（これが「公開」操作）
3. 記事URL: `/{カテゴリスラッグ}/{記事スラッグ}/`（カテゴリは PROJECT.md 参照）
4. `templates/article.html` に以下を組み込む:
   - title / meta description / canonical / OGP
   - JSON-LD: BlogPosting（author=Person・datePublished・**dateModified**）+ FAQPage + BreadcrumbList（手順系はHowTo追加）
   - 目次（TOC）: `scripts/build.py` がH2/H3から自動生成
   - CTAボタン・「この記事でわかること」ボックス・定義ブロックの共通コンポーネント
5. dateModified はビルド時にJSON-LDとページ上の両方に出力（AI検索は鮮度重視のため必須）
6. `scripts/build.py` の責務: articles/*.md → site/{cat}/{slug}/index.html 変換、sitemap.xml 再生成、llms.txt 追記検証

### 3.4 AIO基盤設定（初回のみ・最重要）

**1. robots.txt: AIクローラーの許可確認**

以下がブロックされていないことを確認（ブロックされているとSEO順位が高くてもAI検索に引用されない）:

```
User-agent: GPTBot
Allow: /

User-agent: OAI-SearchBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: Bingbot
Allow: /
```

注意: Google AI Overview / AIモードは通常のGooglebotのクロール結果を使用する。**Googlebotを絶対にブロックしないこと**。Google-Extended はGemini学習用の制御であり、AI Overview表示可否とは別物。

robots.txt は `site/robots.txt` として管理する（デプロイでそのまま公開される）。

**2. llms.txt の設置**

`site/llms.txt` としてMarkdown形式で設置（サイトルートで公開される）:

```markdown
# サイト名
> サイトの概要を1-2文で記述（誰向けの、何のメディアか）

## 運営者情報
- 運営会社・著者の実績・専門性を記述（E-E-A-Tの明示）

## 主要コンテンツ
- [記事タイトル](URL): 記事の1行説明
```

Phase 6 が新規記事公開のたびに自動追記する。

**3. IndexNowキー設置（オプション）**: `site/{INDEXNOW_KEY}.txt` としてキーファイル設置。

**4. GA4: AI参照元の計測設定**

以下のリファラーを「AI検索流入」として分類:

```
chatgpt.com | chat.openai.com | perplexity.ai | gemini.google.com | copilot.microsoft.com | claude.ai
```

**5. GSC: 生成AIパフォーマンスレポートの確認**

AI Overview・AIモードのインプレッションとページ別引用状況が確認できることを確認。現時点ではインプレッション中心のデータのため、GA4のAI参照元計測と必ず併用する。

---

## 4. 自動化パイプライン定義（全7Phase）

各Phaseの完了を確認してから次に進むこと。

### Phase 1: KW選定エージェント

**目的**: 次に書く記事のキーワードを決定する。

-1. **在庫の作り方（ラッコキーワードが使えるとき）**
   ```bash
   python scripts/kw_plan.py --site <id>            # 計画を作って見る（docs/kw-plan-<id>.md）
   python scripts/kw_plan.py --all --replace         # 台帳の未着手を対象外にして積み直す
   ```
   サブジェクト（`sites/<id>.json` の `kw_seeds.industries`）ごとに、ラッコの
   サジェスト・関連語（月間検索数・SEO難易度つき）と、自サイトのGSC実績を集めて
   点をつける。点は「外注・比較を考えている語（+3）／開く理由／検索数（対数×0.6）／
   難易度の低さ／12か月の伸び／自サイトの表示実績」の合計。**検索数だけで並べると、
   「請求書 封筒 書き方」（自分でやる人）が「経理代行 費用 相場」（外注を考える人）の
   上に来る。** 目的はリードなので買い手の語を先に置く。

   **狙う業種の優先度**は `kw_seeds.priority` で指定する。優先業種は枠が厚く（20本／他は10本）、
   点にも加点される（+1.5）。AI集客ラボは「クリニック・歯科医院・不動産・リフォーム・工務店」
   （月1件の成約で集客支援の費用が回収できる業種）、補助金は「個人事業主（売上の大きい層）・
   中小企業全般」。他業種の起点も残し、汎用性は捨てない。読者像（`audience`）も同じ方針で書く
   — 執筆側がこれを読んで記事の想定読者を決める。

   起点が業種名のサイト（AI集客ラボ・補助金）は、業種を単体でラッコに聞くと患者・
   消費者の検索しか返らない。`kw_seeds.core`（例: 集客/MEO/AIO/SEO）を業種に掛けて聞く。
   `core` が無ければ `owns` の先頭4語を使うが、「クリニック llmo」のような組ができるので
   **業種起点のサイトは必ず `core` を書く**。

   入れない語: 担当領域語（owns）を含まない／他サイトの領域語／指名検索／既存記事と
   食い合う／**求人・副業・資格・儲かる**（ラッコのLSIは重要度highで返すが読者は顧客でない）。
   語順・助詞が違うだけの語は同じ検索とみなす。

   **ラッコの消費は1サイト1回 400 クレジットまで**（`kw_plan.CREDIT_CAP`）。自動課金を
   入れているため、尽きる代わりに請求が伸びる。実測は corporate 約35 / ai-lab 約75 /
   subsidy 約170 で、400 を超えるのは何かが暴走しているとき。使った量は計画ファイルの
   冒頭に残る。クレジットが尽きた場合（402）は打ち切って無料のサジェストで続け、
   記事作成は止まらない。

   **ルール（必ず守る・仕組みで守る）: 課金の前に構成と正確性を固め、ラッコは最後に1回だけ使う。**
   試行錯誤の途中で本番のAPIを叩き直さない。実際に1日で約3,000クレジットを使った。
   **主因は一括調査の「SEO難易度」同時取得**（1語 +0.5、API 1.5倍で 0.75。500語で 390）。
   検索数だけなら500語で15。難易度はサジェスト・関連語の応答に付いてくる分で足りるので、
   一括では取らない（`seoDifficulty: False`）。ラッコの単価は「機能ごと」ではなく
   「語ごと」に付くものがある。**新しい機能を使う前に、必ず1回だけ叩いて `meta.consumedCredit` を見る。**
   - `kw_plan` は課金の前に必ず見積もり（`RAKKO_EST`）を出し、1回の上限（400）か
     月の目安（1,000）を超えるなら取得しない（`RAKKO_GUARD=over`）。手で走らせても同じ
   - 条件を直すときは `--dry-run`（課金なし）で構成を確かめてから本番を1回だけ回す
   - 月次のCIは dry-run を先に記録してから本番に入る。週次補充も月の目安を超えたらラッコを使わない

   **定常運転（毎回クレジットを使わない）**:
   | いつ | 何を | 課金 |
   |:--|:--|:--|
   | 週次（月曜） | `kw_discover --append`（伸びている語の補充） | 応答が30日キャッシュされるため、同じ起点は**月1回**（3サイトで約126） |
   | 月次（1日） | `kw_plan --all --if-needed --replace` | **未着手が40本未満のサイトだけ**。1サイト約35〜170、上限400 |
   | 常時 | `data/rakko_volume.json` の検索数を90日使い回す | 一括登録（最低15）を繰り返さない |

   目安は**月1,000クレジット以内**（プランの1/3）。`python scripts/rakko.py --check` が今月の
   合計を出し、超えると週次の検査が「要対応」として通知に載せる。**条件を直すときは取り直さない**
   — 応答は `data/rakko_cache/` に残っているので、フィルタの調整は課金なしで何度でもできる。
   実際、これをせずに1日で約1,200クレジットを使った。

   `--replace` は非破壊: 「未着手」を「対象外」に変えるだけで、公開済み・執筆中は触らず、
   行も消さない。優先度A/B/Cは点の上位1/3ずつで、管制塔の `next_kw` はこの順に返す。
   週次の `kw_discover` は、この計画を土台に「伸びている語」を毎週足していく。

0. **食い合い審査（最初に実行・必須）**
   ```bash
   python scripts/kw_guard.py "<KW>" --site <site_id>
   ```
   終了コードで機械的に判定する。**LLMの目視判断で代替しない。**
   | 終了コード | 判定 | やること |
   |:--|:--|:--|
   | 0 | 着手可 | Phase 2 へ進む |
   | 1 | 要差別化 | 切り口をずらす。ずらせないなら次のKWへ |
   | 2 | **着手禁止** | 新規で書かない。既存記事を書き足す（リライト扱い） |

   検査するのは3つ。**タイトルの似かただけを見ても防げない**（実際、タイトル類似80%のゲートを通ったのに、狙う範囲が8記事と重なって51語・表示484回でクリック0になった記事がある）。
   - 既存記事の `keyword:` と表記ゆれを吸収した一致（「aio 診断」＝「aio診断」）
   - **GSCの実績**: その語で既に順位を持つ自社ページの有無（最も確かな指標）
   - 主題の重なり（参考値。文章の似かたは弱い指標のため止めない）

   管制塔側でも同じ検査が働く。`add_kw` は表記ゆれを吸収して重複を弾き、`claim_kw`（執筆開始）は同じ語が台帳にあると失敗する。**3サイト共通の台帳なので、他サイトが持つ語も弾かれる。**

1. **記事作成ログの確認**
   - 「記事作成ログ」タブのステータス「未着手」の一番上の行を取得
   - 未着手行あり → そのKWで Phase 2 へ（Ahrefsリサーチ不要）
   - なし → 以下でKW選定を実行
2. **競合KW抽出（3C分析）**
   - Ahrefs `site-explorer-organic-competitors` で競合を自動発見
   - 競合3-5サイトの `site-explorer-organic-keywords` でKW抽出
   - フィルター: country=JP, volume>=200, position<=20
   - 自社の強み（Company）で独自価値を出せるKWを優先
3. **KWデータ取得**
   - `keywords-explorer-overview` で Volume / KD / CPC / Intent 取得
   - select: keyword, volume, difficulty, cpc, traffic_potential, intents / country: JP
4. **AI Overview表示状況チェック【AIO対応】**
   - 候補KWごとに `serp-overview` でAI Overview表示クエリかを判定し「KW戦略」タブG列に記録
   - 判断ルール:
     - **表示あり** → オーガニックCTR低下を織り込む。Info系なら「引用獲得」を主目的に設計。CV遠のInfo系単独狙いは優先度を下げる
     - **表示なし**（Do/Buy・ローカル・YMYL等） → 従来SEOのCTRが維持されるためCV直結KWとして優先度を上げる
     - どちらも「AI Overviewに引用される+クリックでも選ばれる」二段構え前提でPhase 3に引き継ぐ
4.5. **「開かないと済まない語」かの確認【必須】**
   ```bash
   python scripts/kw_intent.py "<KW>"
   ```
   同じ順位でもクリック率は5倍違う。28日の実測で、AI集客ラボの記事は6〜10位でCTR 1.04%、
   補助金サポートは同じ帯で2.80%だった。差は順位ではなく検索語の性質にあった。

   | 判定 | 例 | 扱い |
   |:--|:--|:--|
   | 強 | 不採択理由 / 書き方 / 対象条件 / トラブル | そのまま書く |
   | 並 | 業種×手法の掛け合わせ | 書いてよい |
   | 弱 | 例文 / 診断 / とは / 一覧 | **開く理由を用意してから書く** |

   「弱」の語は、検索結果に答えが1つ出た時点で用が済む。実際「口コミ 返信 例文 医療」は
   8.5位で表示88回・クリック0だった。書く場合は、**検索結果に出せないもの**
   （違反になる例・失敗事例・自社の一次データ）をタイトルと冒頭に置き、
   Phase 3の構成事前審査でその箇所を明示すること。

5. **SEO Knowledge批判3周** — 以下の10観点で批判的レビューを3周:
   1. ロングテール優先（Vol 1,000以下）に沿っているか
   2. CV距離（Do/Buy優先）の分類は正しいか
   3. カニバリゼーションリスクはないか（parent_topic確認）
   4. KD値の信頼性（SERP上位のDRで検証）
   5. トピカルオーソリティ設計（クラスター先→ピラー後）
   6. 競合SERPでDR低サイトが勝てている実績はあるか
   7. 独自性・E-E-A-Tを出せるKWか
   8. 攻め順序は妥当か
   9. 抜けているKWカテゴリはないか
   10. 【AIO】AI Overview表示クエリの場合、引用を獲れる独自ファクト・一次情報を用意できるKWか（「二重負け」KWになっていないか）
6. **KW確定・スプレッドシート更新**
   - 「KW戦略」タブに確定KW記録（G列: AI Overview表示状況含む）
   - 「記事作成ログ」タブに新行追加（ステータス: KW選定完了）

### Phase 2: リサーチエージェント

**目的**: 記事に独自性を持たせる一次情報（X投稿+YouTube文字起こし）を収集する。
**AIO観点**: 一次情報・独自データはAI検索が最も引用したがる素材。ここで集めた固有ファクトが「自サイトが引用される」決定打になる。

0. **収集の実行**: `python scripts/research.py "<KW>" --site <site_id>` を必ず先に実行する。
   YouTubeの文字起こしと自社の一次情報がまとめて出る（YouTubeはAPIキー不要）。
   保存された `data/youtube_transcripts/*.txt` を**執筆前に読む**こと。
   自動字幕は誤変換があるため、数値・固有名詞をそのまま引き写さない。

1. **X（Twitter）は使わない**。検索APIがBasicプラン（月100ドル〜）以上で、
   YouTubeの字幕から同等以上の一次情報が無料で取れるため見合わない。収集コードも削除済み。
2. **YouTube一次情報の収集**
   - YouTube Data API v3で関連動画上位10本を検索
   - `yt-dlp --write-auto-sub --sub-lang ja --skip-download -o "data/youtube_transcripts/%(id)s" "VIDEO_URL"`
   - 文字起こしを `data/youtube_transcripts/{videoId}.txt` に保存し「YouTube一次情報」タブに記録
   - **`python scripts/yt_quotes.py "<KW>"` で引用できる一文だけを抜き出す**。
     数万字の字幕をそのまま読んでも執筆に使われない（実際、字幕12本に対し引用0本だった）。
     数字を含む発言・体験・つまずきの3種に分けて出るので、そこから選んで本文へ組み込む
3. **数値ファクトの抽出【AIO対応】**
   - 「数値で語れるファクト」を3つ以上抽出してメモ化（例:「導入企業のN%が〜」）
   - AIは数値付き断定文を優先的に引用するため、Phase 4で出典付きで配置
4. **API 503エラー時**: 即フォールバックせず10分間隔でポーリング。3回連続503のみエラーログに記録して次工程へ
5. **記事作成ログ更新**: ステータス「一次情報収集完了」

### Phase 3: 設計エージェント（最重要Phase）

**目的**: 検索上位記事の構造分析→共通パターン+独自パートで構成確定。AI Overviewに引用されやすい「回答抽出可能な構造」を織り込む。

1. **検索上位10記事のH2/H3構造抽出**
   - `serp-overview` で上位10記事のURL / DR / タイトル取得
   - 全10サイト（最低7サイト）をフェッチしH2/H3抽出、スプレッドシートに書き出す
2. **AI Overview引用元の分析【AIO対応】**
   - AI Overview表示KWの場合、引用されているURLを確認
   - 引用ページの共通構造を分析: 冒頭直接回答 / 定義ブロック / 比較テーブル / FAQ / リスト構造 / 鮮度表記
   - 「10位以内だが引用されていないページ」との構造差分を特定し、引用されている側の構造を採用
   - 非表示KWの場合は強調スニペット獲得ページの構造を代わりに分析
3. **共通パターン分析**
   - 全サイトのH2をカテゴリ分類し出現率を算出
   - **共通60-70%**: 出現率50%以上のH2は踏襲（検索意図の核心）
   - **独自30-40%**: 出現率25%以下 or 0%のH2を一次情報から設計（差別化）
4. **Query Fan-Out設計【AIO対応】**
   - 共起語・サブトピックを10個設計し、本文で網羅する（トピック網羅性）
   - メインKWから派生するサブクエリを6-8個洗い出す
   - 各サブクエリに1つのH2が対応（1つのH2 = 1つの質問への完結した回答）
   - H2は「質問型」または「回答が予測できる名詞句型」（例:「◯◯とは？」「◯◯の選び方3ステップ」）
   - 各H2直下の「1文結論（40-60字の自己完結アンサー）」草案をこの時点で作成
5. **一次情報の配置確定**: 確定H2ごとに使うX/YouTube一次情報・数値ファクトを決定。メディア埋め込み可否も判断
6. **構成事前審査（執筆前ゲート・最重要）**: 以下の14項目を構成段階で自己審査し、**全項目○になるまで構成を修正してからPhase 4へ進む**。執筆後の修正ループより構成段階の修正のほうが圧倒的に安く確実なため、1項目でも×のまま執筆を開始することを禁止する。
   | # | 構成チェック項目 | 対応する採点観点 |
   |:--|:--|:--|
   | 1 | タイトル案が15〜45字・KW先頭・数字入り | SEO / 機械ゲート |
   | 2 | メタディスクリプション案が60〜160字・検索意図に回答 | SEO / 機械ゲート |
   | 3 | **`kw_guard.py` を見出し案つきで再実行し、終了コード0**（下記） | カニバリゲート |
   | 4 | H2が6〜8個で、各H2がQuery Fan-Outのサブクエリに対応（質問型/名詞句型） | AIO |
   | 5 | 全H2に1文結論草案（40〜60字・単体で意味が通る）を書き終えている | AIO / SEO |
   | 6 | 冒頭200字の断言型回答の草案がある | AIO |
   | 7 | 定義ブロック・比較テーブル・FAQ 5問の配置場所が決まっている | AIO / デザイン |
   | 8 | 出典付き数値ファクトの候補が3つ以上リストアップ済み（出典URL付き） | AIO / 技術 |
   | 9 | 一人称の体験・観察を入れる箇所が2箇所以上決まっている | 編集 |
   | 10 | 失敗例・注意点のH2（またはセクション）がある | AIO |
   | 11 | 内部リンク先3本以上を**公開済み記事から**選定済み | SEO / 連鎖隔離防止 |
   | 12 | 図解の計画（diagrams: 宣言のflow/list/vs型と内容）が決まっている | デザイン |
   | 13 | 各H2の想定文字数の合計が5,500字以上（バッファ込み） | 機械ゲート |
   | 14 | 対象読者の限定文言・鮮度表記の文面が決まっている | AIO / ペルソナ |
   - 審査結果は「○×+×の修正内容」を実行ログに残す（Phase 5で不合格になった場合の原因分析に使う）
   - **項目3の実行方法**（構成が固まった時点で必ず実行する）:
     ```bash
     python scripts/kw_guard.py "<KW>" --site <site_id> \
       --title "<タイトル案>" --h2 "<H2案1>" --h2 "<H2案2>" ...
     ```
     Phase 1では語だけを見た。ここでは**見出し案を1本ずつGSCの実績に当てる**。
     タイトルと狙う語を差別化しても、見出しが既存記事の主題まで伸びていればそこで食い合う。
     「見出しの主題を既存ページが取っている」と出た見出しは、**削るか、要約1〜2段落にとどめて本文はそのページへリンクする**（ピラー記事の正しい形）。
     終了コード1以上のまま執筆を開始することを禁止する。
7. **ユーザー確認**: 確定H2構成（+1文結論草案）を提示し承認を得てからPhase 4へ。自動実行モードはスキップ可
8. **記事作成ログ更新**: ステータス「記事設計完了」

### Phase 4: 執筆エージェント

**目的**: 人間に読みやすく、AIが正確に抽出・引用できる本文を生成する。

1. **80%ドラフト生成**
   - 確定H2構成に沿って生成。一次情報を自然に組み込む（全体の20%以上）
   - 独自ファクト3箇所以上（数値ファクト優先）
   - **文字数は検索意図で決める**。フロントマターの `depth:` を必ず設定する
     - `quick`（3,000字〜）: 手順・定義・単一の疑問への回答
     - `standard`（5,000字〜）: 比較検討・選び方（既定）
     - `deep`（8,000字〜）: 網羅的なガイド・ピラー記事
     全記事が同じ長さに揃うと、それ自体が量産の指紋になる。上限は設けない
2. **AIO対応ライティング【必須実装】**
   - **冒頭200字の断言型直接回答**: 「◯◯は◯◯です」で書き始める。前置き・挨拶・一般論禁止
   - **各H2直下の1文結論**: 40-60字。その1文だけ切り出しても意味が通ること
   - **定義ブロック**: 「◯◯とは」を1-2文で明示、太字/枠囲みで視覚化
   - **ファクトと意見の分離**: 事実と体験談を同じ文に混ぜない
   - **情報鮮度の明示**: 冒頭付近に「2026年◯月時点の情報です」。変わりやすい情報には個別に時点を添える
   - **対象読者の限定文言**: 「この記事は◯◯向けです」を冒頭ボックス付近に明記
   - **代名詞の抑制**: 「これ」「それ」多用禁止。切り出し1文単体で主語が分かる書き方
3. **AI感排除チェック（7項目）** — 第6章の表を必ず全チェック
4. **文章ルール** — 第6章参照（ですます調 / 1文50字以下 / 段落3行以内 / 文長バリエーション / 体言止め2-3箇所 / H2直後の1行目は1文結論）
5. **X引用スタイル**
   - 自分の体験・感想を先に述べ、後から「〜氏も同様のことを言っています」と軽くリンク
   - blockquote禁止。インラインで自然に組み込む
   - `@username` は `<a href="https://x.com/username" target="_blank" rel="noopener">@username</a>` 形式
6. **SEO Knowledge品質批判10-20周**: 検索意図整合 / E-E-A-T / 内部リンク候補 / 独自性 / 読者が行動できるか / 【AIO】各H2が単体で「質問→完結した回答」になっているか
7. **記事作成ログ更新**: ステータス「執筆完了」

### Phase 5: 品質エージェント

**目的**: SEO+AIO最適化と多角的品質チェックで基準スコア達成。

1. **メタ情報設定**: タイトル30文字以内・KW先頭・数字含む（鮮度KWは【2026年最新】検討）/ メタディスク120文字以内 / 冒頭にKW含む太字結論文
2. **E-E-A-T注入**: 著者情報（運営者名・肩書き・実績、「◯年の実務経験から」等）/ データ引用（Ahrefs・公的統計・権威ソース）/ 自社事例
3. **内部リンク・外部リンク**: クラスター内内部リンク3-5本 / 外部権威リンク3-5本 / アンカーテキスト「こちら」禁止（具体表現）
   - **リンク先は公開済み（score 90以上）の記事のみ**。未公開・審査中の記事URLをリンク候補に含めない
   - 保険として build.py が未公開記事へのリンクを自動テキスト化する（リンク先の公開後、再ビルドで自動復活）。隔離記事の生成HTMLも自動削除されるため、連鎖隔離は発生しない
4. **AIO/LLMO/GEO構造対応**: 冒頭200字断言回答 / 各H2冒頭1文結論 / 定義ブロック→数値ファクト3箇所（出典付き）→比較テーブル→Q&A / Query Fan-Out対応H2構成 / FAQ Schema 5問以上（回答40-60字・本文と完全一致）/ 鮮度表記とdateModified整合 / 失敗例・注意点セクション最低1箇所
5. **6エージェント品質チェック（120点満点中114点以上=95%が公開条件）** — 各20点満点:

   **採点プロトコル（甘さ・ブレを防ぐ実施ルール）**:
   1. **機械採点を先に**: `python scripts/score_check.py <slug>` を実行し、数えられる18項目（文字数・マーカー・FAQ・リンク・H2構造・AI感NGワード等）を**全PASSにしてから**LLM採点に入る。機械で測れる項目をLLMに採点させない
   2. **初見の読者として読む**: 採点時は執筆の経緯を忘れ、公開HTMLを初めて読む読者・審査員の視点で本文だけを評価する
   3. **批判先出し**: 各エージェントは採点前に必ず「この記事の欠点・改善点を3つ以上」列挙してから点数をつける（欠点ゼロの採点は無効とし、やり直す）
   4. **足切り**: 合計114点以上でも、**いずれかのエージェントが16/20未満なら不合格**（1観点の壊滅を合計点で覆い隠さない）
   5. **内訳の記録**: 確定スコアはフロントマターに `score:`（100点換算）と `score_breakdown:`（6観点の素点）の両方を記録する
      ```yaml
      score: 95
      score_breakdown: {design: 19, seo: 19, editorial: 19, expert: 18, persona: 19, aio: 20}
      ```


   **エージェント1: デザイン（ビジュアル・フォーマット）** — 各2点×10項目:
   H2構成が目次対応+各H2冒頭1文結論 / テーブルスタイル統一 / CTA 2箇所以上 / H2直下図解 / strong 12-18箇所 / 段落3行以内 / 箇条書き前後のリード文 / プロンプト例コードブロック / alt設定 / フォーマット統一

   **エージェント2: SEO（SEO・コンテンツ品質)** — 各2点×10項目:
   タイトルKW自然含有・30字以内 / メタディスク120字以内・検索意図 / 5,000字以上 / 各H2冒頭1文結論 / 外部権威リンク / E-E-A-T要素 / FAQ 5問以上 / 内部リンク3本以上 / 検索意図と構成一致 / 独自ファクト3箇所以上

   **エージェント3: 編集者（文体・品質)** — 各2点×10項目:
   AI感排除7項目 / ですます調統一 / リード文2-3文 / 一人称体験談2箇所以上 / X・YouTube引用インライン / 専門用語の平易説明 / 1文50字以下・段落3行以内 / 文末バリエーション / 体言止め2-3箇所 / 「この記事でわかること」ボックス

   **エージェント4: 技術専門家（技術正確性)** — 各2点×10項目:
   ツール説明の正確性・最新性 / プロンプト例の実用性 / 手順の再現性 / 概念の平易説明 / 限界・注意点 / 事例数値の信頼性 / 比較情報の正確性 / 次アクションの具体性 / 最新動向と齟齬なし / ユースケース具体性

   **エージェント5: ペルソナ（読者UX)** — 各2点×10項目（ペルソナ定義は PROJECT.md）:
   リード文で悩みに答える実感 / 専門用語説明 / H2結論だけで得られるものが分かる / 事例の再現可能感 / 「まず何から」明確 / 段落3-4文以内 / 表の単体完結 / FAQがペルソナの疑問と一致 / CTA文脈 / 読後の次アクション明確

   **エージェント6: AIO/LLMO（AI検索引用性)** — 各2点×10項目:
   冒頭200字断言回答 / H2冒頭1文結論40-60字・単体で意味が通る / 定義ブロック視覚化 / 比較テーブル単体完結 / FAQ 5問以上・回答40-60字・本文整合 / 数値ファクト出典付き3箇所以上 / 鮮度明示とdateModified整合 / H2がQuery Fan-Out対応の質問型/名詞句型 / ファクト・意見の文単位分離 / 対象読者限定文言+失敗例セクション

   **修正ループ**:
   ```
   採点 → 合計 < 114点の場合:
     1. 最低スコアのエージェントの指摘を優先確認
     2. 指摘事項を上から順に修正
     3. 修正したエージェントのみ再採点（他スコア維持）
     4. 114点以上になるまでループ（最大3サイクル）
     5. 3サイクル後も未達 → ユーザーに差分報告して手動判断
   ```
6. **記事作成ログ更新**: ステータス「品質チェック完了」（I列に「スコア/120」）

### Phase 6: 公開エージェント

**目的**: 画像生成→HTML生成（メタ+Schema直書き）→Cloudflare Pagesへデプロイ。

1. **アイキャッチ画像生成**: `python scripts/make_images.py <slug>` / 16:9 / カテゴリカラー準拠 / タイトルを自動折り返し・自動縮小 / `site/images/{slug}/` に配置
2. **H2直下の図解画像生成**（目安3-5枚）
   - イラストonlyの高品質フラットベクターイラスト
   - **英語テキスト禁止**: プロンプトに「No English text whatsoever. Japanese text only or no text at all.」必須
   - 生成後に目視チェック、英語混入は再生成。altテキスト必須
   - 【AIO】図解の重要情報は必ず本文テキストでも記述（AIは画像を引用できない）
3. **HTML生成（`scripts/build.py`）**
   - `articles/{slug}.md` → `site/{カテゴリ}/{slug}/index.html`（`templates/article.html` 適用）
   - Python `markdown` ライブラリ（extensions: tables, extra）で変換
   - 画像は `<figure>` + `<figcaption>`。見出し階層（H2→H3→H4）のネスト検証
   - 目次（TOC）をH2/H3から自動生成して冒頭に挿入
4. **メタ情報の直書き**（テンプレート変数で注入）
   - `<title>`: SEOタイトル（KW先頭・30文字以内）
   - `<meta name="description">`: 120文字以内
   - `<link rel="canonical">` / OGP（og:title, og:description, og:image=アイキャッチ）
5. **JSON-LD Schema直書き【AIO対応】**（テンプレートに埋め込み）
   - BlogPosting: headline / author（Person: 名前・肩書き・プロフィールURL） / datePublished / **dateModified** / image
   - FAQPage: 本文FAQセクションと**完全一致**（不一致はスパム判定・AI不信頼リスク）。回答40-60字目安
   - BreadcrumbList: トップ > カテゴリ > 記事
   - HowTo: 手順系記事のみ追加（各ステップ1-2文）
6. **サイトメタファイル更新【AIO対応】**
   - `site/sitemap.xml` にURL追加（build.pyが再生成。lastmod=公開日）
   - `site/llms.txt` に `- [記事タイトル](公開URL): 記事の1行説明` を追記
   - トップページ・カテゴリ一覧ページに新着記事リンクを追加
7. **ローカル検証**
   - 生成HTMLをブラウザ確認（レイアウト崩れ・画像リンク切れ・TOC・CTA表示）
   - JSON-LDをリッチリザルトテスト相当の構文チェック（パースエラーなし）
8. **デプロイ（=公開）**
   ```bash
   git add site/ articles/ && git commit -m "publish: {記事タイトル}" && git push
   ```
   - Cloudflare Pages が自動ビルド・配信。数分後に公開URLを確認
   - 通常モード: commit後にユーザー確認→pushで公開。自動実行モードは114点以上で自動push
9. **記事作成ログ更新**: ステータス「公開済み」

### Phase 7: 分析エージェント

**目的**: インデックス登録・AI検索向けURL通知・データ更新・内部リンク追加・AIO計測・KPIレポート。

1. **Google Indexing APIで即時インデックス登録**
   ```python
   from google.oauth2 import service_account
   from googleapiclient.discovery import build

   credentials = service_account.Credentials.from_service_account_file(
       'indexing-service-account.json',
       scopes=['https://www.googleapis.com/auth/indexing'])
   service = build('indexing', 'v3', credentials=credentials)
   service.urlNotifications().publish(
       body={'url': 'https://YOUR_DOMAIN.com/post-url/', 'type': 'URL_UPDATED'}).execute()
   ```
   未設定の場合: GSC URL検査ツールでの手動登録をユーザーに依頼
2. **IndexNow送信【オプション】**
   ```bash
   curl "https://api.indexnow.org/indexnow?url=https://YOUR_DOMAIN.com/post-url/&key=${INDEXNOW_KEY}"
   ```
3. **AIO計測データの取得**
   - GSC生成AIパフォーマンスレポート: AI Overview/AIモードのインプレッション・引用ページ一覧
   - GA4 AI参照元セッション: chatgpt.com / chat.openai.com / perplexity.ai / gemini.google.com / copilot.microsoft.com / claude.ai
   - 「AIO計測」タブに新行追加
4. **スプレッドシート全タブ更新**: ダッシュボード / 記事作成ログ / KW戦略 / トピッククラスター / KPIレポート / AIO計測
5. **既存記事への内部リンク自動追加**
   - 公開済み全記事のH2・KWを取得→新規記事KWを自然に挿入できる箇所を特定
   - 該当する `articles/*.md` を編集→`scripts/build.py` で再ビルド→git pushで反映
   - 「内部リンク管理」タブに記録。アンカーテキストは具体表現（「こちら」禁止）
6. **kpi_feedback.md の更新**: 成功/失敗パターン追記（AI引用の成功/失敗パターン含む）。翌朝のパイプラインが読み込む

---

## 5. 自動スケジュール定義

| スケジュール | 実行時刻(JST) | 内容 |
|:--|:--|:--|
| Article Pipeline | 1日20枠 | Phase 1〜7。**1社2本/日・最大10社**。枠の番号をサイト数で割った余りでサイトを選ぶ |
| Daily KPI Report | 毎日 22:13 | KPIレポート+AIO計測+kpi_feedback.md更新 |
| Weekly Optimize | 月曜 10:23 | カニバリ検出・リライト・内部リンク最適化・AIO監査・**自動修正と見直し（8.6）**・新規KW提案 |
| Monthly AI Citation Check | 毎月1日 11:00 | AI引用スポットチェック |

### Morning/Afternoon Pipeline 実行フロー

1. kpi_feedback.md を読み込む（前日の成功/失敗パターンを反映）
2. Phase 1〜7 を順次実行
3. 実行ログをエラーログタブに記録
4. 完了通知

### Daily KPI Report 実行フロー

1. sitemap.xml と「記事作成ログ」タブで公開記事数を取得
2. GA4でPV・エンゲージメント時間・AI参照元セッション取得
3. GSCで表示回数・CTR・平均順位・生成AIレポート確認
4. サイトマップ登録状況確認
5. 成功/失敗パターン分析（AI引用観点含む）→ kpi_feedback.md 更新
6. 「KPIレポート」「AIO計測」タブに記録→完了通知

### Monthly AI Citation Check 実行フロー

1. 「KW戦略」タブから公開済み記事の主要KW上位20個を取得
2. 各KWのAI検索引用状況をスポットチェック（AI Overview引用リンク / ChatGPT・Perplexity回答出典）
3. 結果を `data/ai_citations/YYYY-MM.json` と「AIO計測」タブI列に記録
4. 「順位は高いのに引用されない」記事をリライト優先度リストに追加

---

## 6. 品質ルール

### AI感排除7項目（全記事必須チェック）

| # | チェック項目 | NG例 | OK例 |
|:--|:--|:--|:--|
| 1 | あいまいな結論 | 「場合もあります」「可能性があります」 | 具体的シナリオ・条件を明示 |
| 2 | 重要ですの連発 | 「〜することが重要です」4回以上 | 記事中3回まで。具体的アクション提示 |
| 3 | 文末パターン同一 | 全段落「〜です。」 | 「です/ます/でしょう/体言止め」交互 |
| 4 | 3連続箇条書き | 箇条書き×3連続 | 間にリード文やテーブル |
| 5 | これにより | 「これにより効率が向上します」 | 完全削除。具体的因果を文章で |
| 6 | おすすめします | 「検討することをおすすめします」 | 「してください」「試してみてください」 |
| 7 | 一人称の欠如 | 第三者視点のみ | 一人称の観察・体験談2箇所以上 |

### AIO/LLMO対応ルール（全記事必須実装）

| # | ルール | 実装方法 |
|:--|:--|:--|
| 1 | 冒頭200字の断言型直接回答 | 「◯◯は◯◯です」で書き始める。前置き禁止 |
| 2 | 各H2直下の1文結論 | 40-60字。単体で切り出しても意味が通る |
| 3 | 定義ブロック | 「◯◯とは」1-2文。太字/枠囲み |
| 4 | 数値ファクト3箇所以上 | 出典付き。一次情報・独自調査優先 |
| 5 | 比較テーブル | 単体で意味が完結。ヘッダー明確 |
| 6 | FAQ 5問以上 | 回答40-60字。本文とSchema完全一致 |
| 7 | 情報鮮度の明示 | 「◯年◯月時点」明記。dateModified整合 |
| 8 | ファクトと意見の分離 | 事実と体験談を同じ文に混ぜない |
| 9 | 対象読者の限定 | 「この記事は◯◯向けです」を冒頭明記 |
| 10 | 失敗例・注意点 | 「◯◯はNG」形式で最低1箇所 |
| 11 | 代名詞の抑制 | 切り出し1文で主語が分かる文にする |
| 12 | 画像依存の禁止 | 図解の重要情報は必ず本文テキストにも記述 |

### 装飾ルール（可読性 — 自動生成記事にも必須適用）

記事Markdownで使える装飾。CSSとbuild.pyが自動でスタイル適用する。

| 記法 | 表示 | 使いどころ |
|:--|:--|:--|
| `**テキスト**` | 黄色マーカー+太字 | 核心フレーズ・数値（1記事12-18箇所） |
| `==テキスト==` | 黄色マーカー（太字なし） | 補助的な強調 |
| `*テキスト*` | 青太字+青点線下線 | 用語・キーワードの強調 |
| `<span class="txt-red">` | 赤太字 | 警告・NG事項（1記事1-3箇所まで） |
| `<span class="txt-blue">` | 青太字 | ポジティブな強調 |
| `<span class="big">` | 1.3倍サイズ太字 | 最重要メッセージ（1記事1箇所まで） |
| H2直下の段落 | 自動で紺色太字・大きめ表示 | 1文結論（書くだけで自動強調） |

- 冒頭の断言段落は自動でリード表示（1.13倍）になる
- 装飾の乱用禁止: マーカーは1画面に2-3箇所が目安。全部を強調すると何も強調されない
- マーカー適用範囲: 記事本文に加え、LP・固定ページの `**太字**`/`<strong>` も自動で黄マーカーになる（ダーク背景・CTA・表ヘッダーは除外）。固定ページの重要文言にも `<strong>` を使うこと
- **生HTMLブロックの中では `**` が効かない**: 注意ボックス（`<div class="caution-box">`）など、行頭が `<` のブロックの中は Markdown が処理しない。`**罰金**` と書くと記事に `**` がそのまま出る（実測282箇所）。ボックスの中は `<strong>` を使う
- **表・箇条書き・番号リストの前には空行を1つ入れる**: 空行がないと前の段落の続きとして扱われ、表がパイプ記号のまま本文に出る（実測62箇所）。`scripts/fix_block_breaks.py` が週次で検出・修復する

### 文章ルール

- 文体: ですます調で統一（「だ/である/〜した」調禁止）
- 1文50文字以下 / 段落3行 or 150字以内 / 文長バリエーション / 体言止め2-3箇所
- H2/H3直後: 必ずリード文1-3行（いきなり表・リスト禁止）。H2直後の1行目は1文結論
- H4見出し: サブステップには `<strong>` ではなくH4タグ
- プロンプト表示はコードブロック形式

### 記事構成共通ルール

- 目次: `scripts/build.py` がH2/H3から自動生成（本文Markdownに手動挿入しない）
- テキスト強調: `<strong>` 12-18箇所（核心フレーズ・数値・アクション喚起に限定）
- CTA: 最低2箇所。中央揃えボタンブロック統一
- 「この記事でわかること」ボックスを冒頭に必ず設置
- FAQ 5問以上+FAQPage Schema
- **CV導線の強化**: ゼロクリック前提で「引用でブランド認知→指名検索→CV」導線として設計。社名・サービス名を数値ファクトとセットで記述し、AI回答内に社名ごと引用される形を狙う

### 画像ルール（本サイトの運用実装）

自動生成でも以下を必ず実行する（画像生成API未設定時のフォールバックツールを用意済み）:

1. **アイキャッチ必須**: 全記事のフロントマターに `eyecatch:` を設定する。生成は **`python scripts/make_images.py <slug>`（推奨・一括）** — フロントマターのtitle/categoryからアイキャッチを自動生成し、`diagrams:` 宣言があれば図解も一括生成する。タイトルは自動折り返し・自動縮小のためはみ出しなし。PNGは自動軽量化される。個別生成は `make_eyecatch.py <slug>` でも可
2. **一覧サムネイル自動**: build.pyがアイキャッチを記事一覧・関連記事のサムネイルとして自動表示（eyecatch未設定時はカテゴリ色のグラデーションにフォールバック）
3. **本文図解**: 手順・構造系のH2には図解を入れる。記事フロントマターに `diagrams:` を宣言して `make_images.py <slug>` で一括生成（推奨）。型は3種: `flow`（手順2〜5）/ `list`（チェックリスト3〜6項目）/ `vs`（NG・OK比較2カラム）。個別生成: `python scripts/make_diagram.py [--type flow|list|vs] <slug> <ファイル名> <タイトル> <項目...>`。テキストは自動縮小されはみ出さない。build.pyが画像の生成漏れを機械検出する
4. **文字化け検証**: 画像生成後は必ずReadで目視確認。日本語が欠け・化けた場合はフォント（YuGothB/meiryob）の存在を確認して再生成
5. **実写素材**: `site/images/company/` の自社素材（ロゴ・代表写真・G-ran資料画像）を優先使用。外部素材は権利確認済みのもの以外使用禁止
6. **ロゴ**: ヘッダー=`logo.png`、フッター/ダーク面=`logo-white.png`、アイコン=`star-white.png`（七の文字ロゴは廃止）

### 画像ルール（一般原則）

- アイキャッチ: フラットイラスト・16:9・カテゴリカラー準拠
- H2直下図解: フラットベクターイラスト、テキストなし or 日本語のみ
- **英語テキスト絶対NG**（プロンプトに「No English text」必須）
- altテキスト必須（KW詰め込みNG）
- 生成後の目視チェック必須。画像依存禁止（AIO/LLMOルール12）

### 機械品質ゲート（build.pyが自動検査 — 警告ゼロが公開条件）

| 検査 | 基準 | 実装 |
|:--|:--|:--|
| タイトル字数 | 15〜45字 | build.py quality_checks |
| メタ記述字数 | 60〜160字 | 同上 |
| カニバリ防止 | タイトル類似80%以上でWARN（差別化 or 統合） | 同上（KW選定段階の機械スキップと二重防御） |
| 内部リンク404 | 全内部リンクの存在検証（404ゼロ保証） | build.py link_check |
| マーカー数 | 強調8箇所以上（推奨12-18） | build.py（記事ごと） |
| 文字数 | 本文5,000字以上（タグ・空白除く実文字数） | build.py（記事ごと） |
| 鮮度・通知 | sitemap lastmod自動 + `scripts/notify_indexnow.py` で即時通知 | Phase 7 / 週次リライト後 |
| **描画の崩れ** | 生成HTMLに生のMarkdown記法・空リンク・alt無し画像が残っていない | `scripts/render_check.py`（build.py が自動で呼ぶ） |
| **取りこぼし** | 原稿にある表・リスト・見出し・画像・リンクが、出力に同じ数だけ出ている | `render_check.structure_gap()`（build.py が自動で呼ぶ） |

**症状の一覧では足りない理由**: 上の描画検査は「こちらが知っている壊れ方」しか見つけられない。実際、4種類の崩れが長期間だれにも気づかれずに公開されていた。そこで**中身を見ず、原稿と出力で塊の数だけを突き合わせる**検査を足した。知らない壊れ方でも、変換に失敗すれば数は合わなくなる。

**原稿ではなく出力を見る検査がある理由**: 上の他の検査は全部、原稿（Markdown）だけを見ている。そのため「原稿としては正しいが、変換すると壊れる」崩れが素通りしていた。実際に見逃したもの — 表がパイプ記号のまま本文に出る（62箇所）・`**` がそのまま表示される（282箇所）・`[文字](/url/)` が押せない形で出る（34箇所）・段落が装飾の内側で分かれて `**` が次の段落へ残る（19箇所）。**どれも記事の点数には出ず、読者の画面にだけ出る。**

### 公開条件（90点未満はアップロード不可 — build.pyが物理的に強制）

1. Phase 5の6エージェント採点（120点満点）を**100点満点に換算**し、記事フロントマターの `score:` に記録する（換算式: `round(合計/120*100)`）
2. **`score` が90点未満、または未記載の記事は build.py がビルド対象から除外**する（サイト・sitemap・feed・一覧のどこにも掲載されない = アップロード不可）
3. build.pyの機械品質ゲートで警告ゼロ（メタ字数・カニバリ・リンク404・マーカー数）
4. 未達は修正ループ（最大3サイクル）→ 再採点して `score` を更新 → なお未達はユーザーに差分報告
- 推奨水準は95点以上。90点は「公開してよい最低ライン」であり目標ではない

---

## 7. KPIレポート定義

### 取得データ

| データソース | 取得項目 |
|:--|:--|
| サイトリポジトリ（sitemap.xml / articles/） | 公開記事数、下書き数、カテゴリ別記事数 |
| GA4 Data API | PV、エンゲージメント時間、直帰率、ページ/セッション |
| GA4（AI参照元） | ChatGPT/Perplexity/Gemini/Copilot/Claude経由セッション数 |
| GSC Data API | 表示回数、クリック数、CTR、平均順位 |
| GSC 生成AIパフォーマンスレポート | AI Overview/AIモードインプレッション、引用ページ一覧 |
| Ahrefs API | DR、被リンク数、オーガニックKW数、推定トラフィック |
| サイトマップ | 登録URL数、最終更新日 |

### 成功パターン（kpi_feedback.md「成功パターン」に追記）

- 公開7日以内にGSC表示回数が発生した記事 → 構造・KW特性を記録
- 品質スコア117点以上の記事 → 特徴（文字数・一次情報数・Schema種類）を記録
- クラスター完成後に全体順位上昇 → クラスター効果を記録
- 【AIO】AI Overview/AIモードでインプレッション発生 → 冒頭回答・H2結論・FAQ・テーブル構造を記録し以降踏襲
- 【AIO】AI参照元セッション発生 → プラットフォームと引用された独自ファクトを記録

### 失敗パターン（kpi_feedback.md「失敗パターン」に追記）

- Vol=200以下のKWで作成 → 月間推定セッション上限が低い
- インデックス未登録 → サイトマップ・Schema・内部リンクの問題特定
- カニバリ発生KW → 対処法（統合 or 差別化）を記録
- 【AIO】順位10位以内なのにAI Overview引用なし → 冒頭回答・抽出構造・鮮度不足を特定しリライト候補に
- 【AIO】AI Overview表示クエリでCTR大幅低下 → 引用獲得+CV導線強化リライトを優先

### kpi_feedback.md 生成フォーマット

```markdown
# KPIフィードバック（自動更新: YYYY-MM-DD）

## サイト概況
| 指標 | 値 | 前日比 | 備考 |
|:----|:--|:------|:----|
| 累計公開記事数 | N本 | +N | |
| ドメインレーティング | N | +N | |
| オーガニックKW数 | N | +N | |
| 推定オーガニックトラフィック | N | +N | |
| AI Overview インプレッション | N | +N | GSC生成AIレポート |
| AI引用ページ数 | N本 | +N | GSC生成AIレポート |
| AI参照セッション | N | +N | GA4 |

## 今日の主な動き
## 成功パターン（これを踏襲せよ）
## 失敗パターン（これを避けよ）
## リライト優先度リスト
| 記事 | 順位 | AI引用 | 改善ポイント | 期待効果 |
```

### 翌朝パイプラインへの反映

Morning/Afternoon Pipeline 冒頭で kpi_feedback.md を読み込み:
1. 成功パターンの構造を新規記事に踏襲（AI引用獲得構造を最優先）
2. 失敗パターン該当要素を回避
3. リライト優先リストがあれば新規記事より優先してリライト実行

---

## 8. 週次最適化定義（Weekly Optimize）

毎週月曜 10:23 実行。

### 8.1 カニバリ検出

1. GSCで全記事のランクインKW取得
2. 同一KWで2本以上ランクイン → カニバリ抽出
3. 対処: 統合（低品質側を削除・301リダイレクト） or 差別化（H1・メタディスク・構成）
4. 【AIO】カニバリはAI検索でも不利（AIが両方落とすことがある）。**統合を基本方針とする**
5. **統合は `auto_merge.py` が週2組ずつ行う**（判断は claude、通してよいかは機械が決める）

   ```bash
   python scripts/auto_merge.py              # 候補を見る（GSCで同じ語に2本が出ている組）
   python scripts/auto_merge.py --write      # 統合する
   python scripts/auto_merge.py --selftest   # 検算が効くか確かめる（週次が先に通す）
   python scripts/retract.py --pending       # 他サイトの配信先から外す分（週次が --push で行う）
   ```

   根拠はGSCの実績だけ。タイトルが似ているだけでは統合しない。**同じ語が2つ以上**で両方が出ていて、
   かつ題名か構成が似ている（または負けた側の狙う語を勝った側が既に扱っている）組だけ。
   1語だけの重なりは偶然が多い（実際「農業機械補助金」と「農業用倉庫の補助金」が1語で挙がった）。
   負けた側（drag）の28日クリックが3回を超える、表示が勝った側より多い、未公開、別サイト、
   のどれかなら見送る。
   吸収後は loser を `articles/_merged/` へ移し、内部リンクを付け替え、301を書く
   （AI集客ラボは `site/_redirects`。他サイトは `data/retractions.jsonl` に積み、
   `retract.py` が配信先の content・manifest・sitemap・llms.txt から外して `_redirects` に足す）。

   検算は auto_rewrite の8つに加えて3つ: **本文が減っていない**／**数字は2本のどちらかにあったものだけ**／
   **loser にしか無かった見出しの語が1つ以上入った**（何もしない統合を通さない）。

### 8.2 リライト対象の選定基準

| 優先度 | 条件 | 対処 |
|:--|:--|:--|
| 最高 | GSC順位11-30位 | H2追加・一次情報追加・内部リンク強化で1ページ目へ |
| 最高 | 順位10位以内なのにAI Overview引用なし | 冒頭断言回答化・H2直下1文結論・FAQ整備・数値ファクト追加・鮮度更新。**`auto_rewrite` の `aio` 種別が週次で自動処理**（`ai_citation_check` の疑い一覧を最優先に取り、登録済みの一次情報の数字だけを足す） |
| 高 | 表示回数高×CTR低 | タイトル・メタディスク改善。AI Overview表示クエリなら引用獲得+CV導線強化 |
| 中 | 公開30日以上&インデックス未登録 | テクニカルSEO確認 |
| 中 | 公開/更新から6ヶ月経過 | 鮮度更新（数値・事例・料金・「◯月時点」・dateModified） |
| 低 | 情報が古い | 料金・機能・バージョン更新 |

### 8.2.5 サイト構成は「手法 × 業種」の2軸で組む

記事を手法（AIO・SEO・MEO・AI集客）だけで分類すると、読者が探している単位と合わない。
実測では、クリニック関連の21本が4カテゴリに散り、まとめて見る場所が無かった。
`data/industries.json` に業種を定義し、`industry_hub.py` が
`/industry/<slug>/` を自動で組む（記事が増えるほどハブが厚くなる。運用の手間は増えない）。

| 決まり | 理由 |
|:--|:--|
| 記事が `_min_articles`（既定5）本に満たない業種はページを作らない | 中身の薄いページはサイト全体の評価を下げる |
| 判定は定義の上から順。具体的なものを先に置く | 「歯科医院」は「医院」を含むため、クリニックより先に置かないと吸われる |
| lead に数字を書かない | 一次情報として登録した数字だけを使う決まり（0.1節） |

**入口ページが孤立していないかを毎回検査する。** 実際に、一次データの入口（`/data/`）へ
内部リンクが1本も無く、誰もたどり着けない状態が続いていた。ゲート
（`test_site_has_two_axes_and_no_orphans`）がトップからの到達性を毎回確かめる。

### 8.3 内部リンク最適化

- 内部リンク3本未満の記事に関連記事リンク追加
- 新規クラスター記事公開時はクラスター内全記事に相互リンク
- 「こちら」アンカーを具体表現に置換。「内部リンク管理」タブ更新

### 8.4 テクニカルSEO監視項目

| 項目 | チェック方法 | 期待値 |
|:--|:--|:--|
| サイトマップ | site/sitemap.xml フェッチ | 公開記事数と一致 |
| robots.txt | フェッチしブロック確認 | AIクローラー6種許可済み |
| llms.txt | フェッチし記事数確認 | 公開記事数と一致 |
| Core Web Vitals | PageSpeed Insights API | LCP<=2.5s, INP<=200ms, CLS<=0.1 |
| **重い読み方の再発** | `speed_fix.leftovers()`（ゲート） | Google Fonts 0件・先読みの計測タグ 0件 |
| HTTPS | SSL証明書期限 | 30日以上 |
| 404 | GSCカバレッジ | 404ゼロ |
| Schema | JSON-LD確認 | BlogPosting+FAQPage+BreadcrumbList（手順系はHowTo） |
| dateModified | サンプル記事Schema確認 | 実際の更新日と一致 |

**表示速度の方針（3サイト共通・`scripts/speed_fix.py`）**: 日本語のWebフォントは使わない
（Noto Sans JP は文字範囲ごとに60〜95本・1.0〜1.4MB読まれ、モバイルの LCP が7〜13秒だった）。
端末のフォント（ヒラギノ／游ゴシック／Noto Sans CJK）で描く。計測タグは load の1.2秒後に読む
（それまでの出来事は dataLayer に溜まる）。本文画像は `loading="lazy"`、アイキャッチは `fetchpriority="high"`。
実測: 記事ページ 総合62→96、LCP 7.3秒→2.1秒、転送1.8MB→0.38MB。

### 8.5 AIO監査（週次）

1. GSC生成AIレポート週次確認: インプレッション推移、引用開始/消失ページの特定
2. 引用消失記事 → 競合引用ページと構造比較し、鮮度更新+構造改善リライト
3. GA4 AI参照元をプラットフォーム別に確認 → 伸びているプラットフォームの重視シグナル（第0章）に沿った施策強化
4. FAQ Schema整合性チェック（サンプル5記事）
5. 結果を「AIO計測」タブと kpi_feedback.md に記録
6. **引用の実測**（月次）: `python scripts/ai_cite_check.py` が主要20語を検索つきAI（ChatGPT / Gemini / Perplexity）に
   投げ、出典に自社URLが入るかを `data/ai_citations/YYYY-MM.json` の `measured` に残す。キーは
   `OPENAI_API_KEY` / `GEMINI_API_KEY` / `PERPLEXITY_API_KEY`（あるものだけ使う。無ければ飛ばす）。
   月1回・1サイト20語まで。試行錯誤で本番を叩かない（`--limit 1` で1語だけ試す）

### 8.6 自動修正と、その見直し（週次・必須セット）

**自動修正は、必ず見直しとセットで実行する。** 当てる側は1本ずつしか見ないため、積み上がりを検知できない。見直しを別工程として必ず通す。

```bash
python scripts/auto_improve.py --write    # 効果測定 → 内部リンクを自動で当てる
                                          # → 完了後 auto_review --fix が自動で走る
python scripts/auto_review.py             # 見直しだけを単独で実行（検査のみ）
python scripts/shorten_anchors.py --write # 長すぎるアンカーテキストを詰める
python scripts/auto_review.py --log       # 何をいつ自動で直したかの台帳
```

**なぜ見直しが要るか**: 1本ずつ正しい修正でも、積み上がると記事を壊す。実測では「関連して、[X]もあわせてご確認ください。」という同じ一文が**サイト全体の内部リンク文970本中403本（41.5%）**を占め、1記事に11本並んだものまであった。1本単位では基準内でも、全体で見れば量産の指紋になる。

| 見る対象 | 基準 | 超えたときの処理 |
|:--|:--|:--|
| 1記事のリンクだけの段落 | 4本まで | 後ろから外す。ただし**送り先の被リンクが5本を割るものは残す**（足しては消すの振動を防ぐ） |
| 1記事の同じ言い回し | 2本まで | link_new の型へ振り直す（リンクは残す） |
| サイト全体の同じ言い回し | 16%まで | 同上。8種の型に散らす（均等でも12.5%になるため、12%は達成できない基準だった） |
| 1つのアンカーテキスト | 26字まで | リンク先は変えず、表示だけ意味の切れ目で詰める |
| 1文の長さ | 100字まで | 左がそれだけで文になる形でだけ分ける（`split_sentences.py`）。「〜によると、」「〜し、」は左が文にならないので触らない |
| 1段落の長さ | 200字まで | 「。」の位置で分ける（`split_paragraphs.py`）。分けた片側は55字以上 |

**自動修正で守ること**:
1. **言い回しを自前で書かない** — 内部リンク文は `link_new.sentence()` の型から選ぶ。スクリプトごとに固有の一文を書くと、その一文がサイト中に並ぶ
2. **当てた後に検算する** — `auto_review.guard()` がHTMLタグの開閉数・リード導線の数・見出し数・本文の減り幅を修正前後で比較し、1つでも崩れていればその記事は書き込まない
3. **リード導線には触れない** — CTA・診断リンク・問い合わせ導線は削除対象から機械的に除外する
3.5. **1回で削りすぎない** — 上限を超えた分を一度に全部外すと本文が7%以上減り、検算で毎回見送られて永久に直らない。1回の削減は本文の6%までとし、週次で回るうちに上限まで収める（実測で27本→4本に収束した）
4. **台帳に残す** — `automation/logs/auto_fix.jsonl` に「いつ・どのスクリプトが・どの記事を・何をしたか」を1行ずつ記録する
5. **ビルドの機械ゲートに通す** — 見直しの最後に `build.py` を自動実行し、通らなければ知らせる。`git checkout -- articles/` で元に戻せる

**文章の判断が要る直し**: タイトルの書き換えと検索意図の見直しは、正規表現では当てられない（主張のずれた記事が量産される）。`auto_rewrite.py` が claude に1本ずつ直させ、**通してよいかは機械が決める**。

```bash
python scripts/auto_rewrite.py            # 何を直すか見る
python scripts/auto_rewrite.py --write    # 直させて検算する（週次で2本ずつ）
```

検算に1つでも外れたら `git checkout` で元に戻す。見るのは8つ。

| 見るもの | 外れたときに起きること |
|:--|:--|
| 触ったのがその1本だけか | 関係ない記事が巻き込まれる |
| 狙う語（keyword）が変わっていないか | 台帳と記事がずれ、食い合い検査が効かなくなる |
| タイトルが15〜45字で狙う語を含むか | 機械ゲートで公開できなくなる |
| **本文に無かった数字が増えていないか** | **事実でない数値が公開される**（減るのは可、増えるのは不可） |
| 出典リンクが消えていないか | 根拠のない主張になる |
| 警告が増えていないか | 直したつもりで別の場所を壊している |
| `kw_guard` が通るか | 既存記事との食い合いを新たに作る |
| ビルドが通るか | サイトが壊れる |

権限は全部は飛ばさない。1ファイルを書き換えるだけなので、使える道具を `Read,Edit` に絞る。

**動かす前に、検算が効くことを確かめる**: 週次は `auto_rewrite.py --selftest` を先に通す。やってはいけない書き換え（無かった数字を書く・出典リンクを消す・狙う語を変える・タイトルを短くしすぎる）を本番の記事に当てて、検算が止めるかを見る。止まらなければ書き換えの工程を飛ばす。**検算が壊れたままエージェントを動かすと、悪い書き換えがそのまま公開される。** 診断は必ず記事を元に戻す。

**それでも自動化しないもの**: 被リンクの依頼（相手がある）、認証情報の再発行（アカウント操作）、料金・事業方向性の判断。

### 8.7 見つけたものを、必ず誰かに届ける

**検査は「見つける」だけでは仕事をしていない。届いて初めて意味がある。**

週次・日次の検査はすべて `|| true` を付けて呼んでいる。1つ止まっても全体を
止めたくないためだが、その結果、終了コードは捨てられ、何を見つけても実行ログの
奥に埋もれていた。実際、週次のSlackは**「週次最適化: success」とリンクだけ**で、
会社表記のゆれもAIO基盤の欠けも、毎週見つけては捨てていた。

```bash
python scripts/findings.py           # 検査をまとめて回し、見つかったものを集める
python scripts/findings.py --quiet   # 生の出力を出さず、まとめだけ見る
```

`automation/logs/findings.txt` に通知用の本文を書き出し、Slackの本文に載る。
GitHub Actions では `::warning::` の注釈になり、run の画面に出る。
**この工程自体は決して止まらない**（常に0で終わる）。知らせるのが仕事で、
止めるのは各検査の仕事。

#### 終了コードの決まり（検査を作るとき・直すときに守る）

| 状態 | 終了コード | 出すもの |
|:--|:--|:--|
| 問題が**見つかった** | **0** | `<名前>_OK=no` と明細行 |
| 問題が無かった | 0 | `<名前>_OK=yes` |
| 検査そのものが**動かなかった** | 1 | 理由 |

**「見つかった」を終了コード1で表さない。** CIのログでは本当に落ちたときと
区別がつかず、実際に一度、正常な検出を不具合と読み違えた。判定は印
（`*_OK=`）で行い、終了コードは「検査が動いたか」だけに使う。

例外は `nap_check` と `deploy_check`。人が手を動かすまで消えない問題のため、
1を返して目立たせる（呼び出し側の `|| true` で全体は止まらない）。

#### 毎週出るのが正常なもの

`data_sanity` の「数字の信頼性」は、直しようのない事実を報告するため毎週出る
（`form_submit` と `lead_capture` が同時に飛ぶ、国が取れない流入がある等）。
**消すべき警告ではなく、数字を報告する前に読む前提条件**として扱う（0.1節）。

#### メールで届けるのは4つだけ

**うまくいった報告は送らない。** 1日2〜3通の成功通知が届くと、本当に見てほしい
回が埋もれる。定時の工程は `--routine` を付けて呼び、知らせることがある回だけ送る。

| 届ける | 経路 |
|:--|:--|
| 問い合わせ | Apps Script（`automation/gas/`）が直接送る。絞り込みを通らない |
| 異常のアラート | `job.status` が failure、または救済後も未解決のとき |
| 月次レポート | `monthly_report.py --email` / `group_report.py --email` が本体を送る |
| 変えたほうが良い点 | `findings.txt` に「要対応」が入った回だけ |

```bash
python scripts/notify_slack.py --routine "定時の報告"   # 知らせることがある回だけ送る
python scripts/notify_slack.py "異常です"                # 必ず送る
python scripts/notify_slack.py --force "手元から送る"    # 手元では既定で送らない
```

**手元での実行は本番の宛先に届かない。** `.env` に `RESEND_API_KEY` と
`LEAD_TO_EMAIL` があるため、動作確認のつもりの実行が info.ai へ本物のメールを
出していた（`（メッセージなし）` と、引用符が壊れて `$icon` だけの本文が実際に
届いた）。`GITHUB_ACTIONS` が無い環境では送らない。

**Slackに切り替えるには**: GitHub Secrets に `SLACK_WEBHOOK_URL` を登録する。
現在は未登録のため、すべての通知がメールで届いている（コード変更は不要）。

---

## 9. スプレッドシート構成（詳細）

### KW戦略タブ
A:KW / B:Volume / C:KD / D:Intent（info/commercial/transactional） / E:CV距離（Do>Buy>Commercial>Info） / F:自社独自価値 / G:AI Overview表示（表示あり/なし/不明） / H:Status / I:備考

### トピッククラスタータブ
A:KW / B:Volume / C:KD / D:カテゴリ / E:クラスター名 / F:種別（ピラー/クラスター） / G:優先度（A/B/C） / H:Status / I:URL

### X一次情報タブ
A:記事# / B:KW / C:投稿者名 / D:投稿日 / E:投稿URL / F:本文（note_tweet全文・200字以上） / G:imp / H:RT / I:Like / J:メディアURL / K:記事への活用方法
※記事間に空行を入れない。

### YouTube一次情報タブ
A:記事# / B:KW / C:動画タイトル / D:チャンネル名 / E:動画URL / F:再生数 / G:文字起こしパス / H:文字起こし完了（○/×） / I:要約 / J:記事への活用方法
※H列が全件「○」になるまでPhase 4に進まない。

### ダッシュボードタブ
A:指標名 / B:値 / C:前日比 / D:備考
主な指標: 累計公開記事数 / DR / 被リンク数 / オーガニックKW数 / 推定トラフィック / インデックス済数 / 今月公開数 / AI Overviewインプレッション / AI引用ページ数 / AI参照セッション

### 記事作成ログタブ
A:記事# / B:制作日 / C:KW / D:タイトル / E:URL / F:カテゴリ / G:Volume / H:品質ループ回数 / I:品質スコア(/120) / J:ステータス / K:公開パス（site/内） / L:備考
※パイプラインは必ず「未着手」行の一番上から開始する。

### KPIレポートタブ
A:日付 / B:総公開記事数 / C:本日公開数 / D:インデックス率 / E:PV / F:表示回数 / G:クリック数 / H:CTR / I:平均順位 / J:AI Overview imp / K:AI参照セッション / L:備考

### AIO計測タブ
A:日付 / B:AI Overview imp / C:AIモード imp / D:AI引用ページ数 / E:ChatGPT参照 / F:Perplexity参照 / G:Gemini参照 / H:Copilot・その他参照 / I:引用スポットチェック結果 / J:備考

### 内部リンク管理タブ
A:リンク元記事 / B:リンク先記事 / C:アンカーテキスト / D:設置日 / E:リンク先URL

### リライトログタブ
A:記事# / B:リライト日 / C:リライト理由 / D:変更箇所概要 / E:リライト前順位 / F:リライト後順位（1週間後） / G:前後のAI Overview引用有無 / H:効果

### エラーログタブ
A:日付 / B:Phase / C:エラー内容 / D:対応内容 / E:解決状態

---

## 10. トラブルシューティング

### API 503エラー
1. 即フォールバックしない → 10分間隔ポーリング（最大6回=60分）
2. 6回連続503: エラーログ記録 → 該当Phaseスキップ（一次情報収集のみ） or 翌日再実行

### 画像生成失敗
画像はローカル生成のため、APIの失敗による欠落は起きない。
1. フォントが見つからない → YuGothB / meiryob の有無を確認
2. 文字がはみ出す → 自動縮小が効かない長さ。タイトルか項目名を短くする
3. 生成漏れ → build.py が機械検出する。`make_images.py <slug>` を再実行

### ビルド・デプロイエラー（静的サイト）
1. build.py エラー → Markdown構文（テーブル・コードブロックの閉じ忘れ）を確認
2. HTML崩れ → テンプレート変数の未置換・見出し階層のネスト崩れを確認
3. Cloudflare Pages デプロイ失敗 → ダッシュボードのビルドログ確認。出力ディレクトリが `site/` か確認
4. 画像リンク切れ → `site/images/{slug}/` への配置漏れ・パスの大文字小文字を確認
5. Schema構文エラー → JSON-LDのエスケープ漏れ（本文引用符）を確認

### スプレッドシートトークン期限切れ
OAuthトークンは通常7日で期限切れ → リフレッシュトークンで再取得 / MCP再認証 / sheets-token.json の更新日確認

### Ahrefs API units枯渇
1. 月次リセット日確認。リセットまでAhrefsクエリ使用禁止
2. KW選定は記事作成ログの未着手行から取得
3. リセット後の優先順位: site-explorer-metrics → organic-keywords → top-pages → rank-tracker-overview

### インデックス登録が進まない
1. site/sitemap.xml のURL数確認
2. build.py のsitemap生成ロジック確認（新記事の追記漏れ）
3. Indexing APIサービスアカウントのGSCオーナー権限確認
4. robots.txt ブロック確認
5. 手動: GSC URL検査ツールで1記事ずつ登録

### AI検索に引用されない場合

前提: AI Overview引用の最大の前提はGoogle検索上位表示。まず順位を確認。

1. **順位10位以内か確認** — 圏外・2ページ目以降なら通常SEOリライト（8.2最高優先度）を先に
2. **robots.txt / クローラーアクセス確認** — GPTBot/OAI-SearchBot/ClaudeBot/PerplexityBot のブロック、WAF/CDN（Cloudflare等）の自動ブロック（アクセスログで403/429確認）
3. **抽出可能構造確認** — 冒頭200字断言回答 / H2直下1文結論 / FAQ・比較テーブル・定義ブロック
4. **情報鮮度確認** — dateModified 6ヶ月以上前なら鮮度更新。「◯年◯月時点」表記追記
5. **Schema確認** — リッチリザルトテストでFAQPage/Article/BreadcrumbListエラーなし、本文との一致
6. **独自性確認** — 「まとめ直し記事」になっていないか。独自数値ファクト・一次情報追加
7. **全て満たしても引用されない** — 競合引用元ページと構造差分分析、ドメイン権威性（被リンク・メディア露出・指名検索）の中長期強化

### GSC生成AIパフォーマンスレポートにデータが出ない

1. レポート導入は2026年6月。プロパティにより反映に時間差あり
2. データなし=引用されていない可能性大 → 上記「AI検索に引用されない場合」を実施
3. インプレッションのみでクリック・クエリ内訳が見えないのは仕様 → GA4 AI参照元とMonthly AI Citation Checkで補完

---

## 付録: AIO対応チェックリスト（公開前最終確認）

- [ ] 冒頭200字が断言型の直接回答になっている
- [ ] 「この記事は◯◯向け」の対象読者明記がある
- [ ] 「◯年◯月時点」の鮮度表記がある
- [ ] 各H2直下に40-60字の1文結論がある
- [ ] H2がサブクエリ6-8個に対応している
- [ ] 定義ブロックが太字/枠囲みで明示されている
- [ ] 出典付き数値ファクトが3箇所以上ある
- [ ] 比較テーブルが単体で意味が完結している
- [ ] 失敗例・注意点セクションがある
- [ ] FAQ 5問以上・回答40-60字・Schema完全一致
- [ ] ファクトと意見が文単位で分離されている
- [ ] 図解の重要情報が本文テキストにもある
- [ ] BlogPosting(author/dateModified) + FAQPage + BreadcrumbList Schema設定済み
- [ ] robots.txtでAIクローラー許可済み（サイト単位・初回のみ）
- [ ] llms.txtに記事を追記済み
- [ ] Indexing API + IndexNow送信済み
